% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_vital_functions.R
\name{plot_growth_curve}
\alias{plot_growth_curve}
\title{Plot Growth Curves}
\usage{
plot_growth_curve(
  fit,
  data = NULL,
  age_col = "age",
  length_col = "length",
  sex_col = "sex",
  sex_labels = c(`1` = "Female", `2` = "Male"),
  age_range = NULL,
  n_points = 100,
  show_data = TRUE,
  show_50_ci = TRUE,
  data_alpha = 0.4,
  data_size = 1.5,
  ribbon_alpha = 0.2,
  ribbon_alpha_50 = 0.3,
  line_width = 1.2,
  colors = NULL,
  colorblind = FALSE,
  facet_sex = TRUE,
  x_lab = "Age (years)",
  y_lab = "Length (cm)",
  title = NULL,
  k_based = FALSE,
  which_model = 1L,
  base_size = 12,
  theme_args = list(),
  additional_layers = list()
)
}
\arguments{
\item{fit}{A CmdStanMCMC object from \code{\link{fit_bayesian_growth}}.}

\item{data}{Optional data.table/data.frame with observed data for overlay.}

\item{age_col}{Character. Column name for age in \code{data}. Default "age".}

\item{length_col}{Character. Column name for length in \code{data}. Default "length".}

\item{sex_col}{Character. Column name for sex in \code{data}. Default "sex".}

\item{sex_labels}{Named character vector. Labels for sex in plot legend and facets.
Default \code{c("1" = "Female", "2" = "Male")}. Can be customized for other
languages, e.g., \code{c("1" = "Femelle", "2" = "Mâle")} (French),
\code{c("1" = "Hembra", "2" = "Macho")} (Spanish), or
\code{c("1" = "メス", "2" = "オス")} (Japanese).}

\item{age_range}{Numeric vector of length 2. Age range for predictions.
If \code{NULL}, auto-determined from data or defaults to c(0, 30).}

\item{n_points}{Integer. Number of points for prediction grid. Default 100.}

\item{show_data}{Logical. Overlay observed data points? Default \code{TRUE}.}

\item{show_50_ci}{Logical. Show 50\% credible interval ribbon? Default \code{TRUE}.}

\item{data_alpha}{Numeric. Transparency for data points. Default 0.4.}

\item{data_size}{Numeric. Size for data points. Default 1.5.}

\item{ribbon_alpha}{Numeric. Transparency for 95\% CI ribbon. Default 0.2.}

\item{ribbon_alpha_50}{Numeric. Transparency for 50\% CI ribbon. Default 0.3.}

\item{line_width}{Numeric. Width of median line. Default 1.2.}

\item{colors}{Character vector of colors. If \code{NULL}, uses \code{vital_colors()}.}

\item{colorblind}{Logical. Use colorblind-safe palette? Default \code{FALSE}.
When \code{TRUE}, overrides \code{colors} with \code{vital_colors(, "sex_cb")}
for two-sex models or \code{vital_colors(, "okabe_ito")} otherwise.}

\item{facet_sex}{Logical. For two-sex models, facet by sex? Default \code{TRUE}.}

\item{x_lab, y_lab}{Character. Axis labels.}

\item{title}{Character. Plot title. Default \code{NULL}.}

\item{k_based}{Logical. Was the model k-based? Default \code{FALSE}.}

\item{which_model}{Integer. 1=VBGM, 2=Gompertz, 3=Logistic. Default 1.}

\item{base_size}{Numeric. Base font size for theme. Default 12.}

\item{theme_args}{List of additional arguments for \code{ggplot2::theme()}.}

\item{additional_layers}{List of additional ggplot2 layers to add.}
}
\value{
A ggplot object.
}
\description{
Creates publication-quality growth curve plots with credible intervals.
Automatically handles single-sex and two-sex models.
}
\examples{
\dontrun{
# Basic usage
plot_growth_curve(growth_fit, data = shark_data)

# French labels
plot_growth_curve(
  growth_fit,
  data = shark_data,
  sex_labels = c("1" = "Femelle", "2" = "Mâle")
)

# Spanish labels
plot_growth_curve(
  growth_fit,
  data = shark_data,
  sex_labels = c("1" = "Hembra", "2" = "Macho")
)

# Customize appearance
plot_growth_curve(
  growth_fit,
  data = shark_data,
  colors = c("#E63946", "#1D3557"),
  theme_args = list(legend.position = "bottom"),
  additional_layers = list(
    geom_hline(yintercept = 100, linetype = "dashed")
  )
)
}

}
\seealso{
\code{vignette("visualization")} for comprehensive plotting examples.

\code{vignette("fit_bayesian_growth")} for growth model fitting.

\code{\link{compare_growth_models}}, \code{\link{plot_residuals}},
\code{\link{vital_colors}}, \code{\link{theme_vital}}
}
