% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bayesian_growth.R
\name{fit_bayesian_growth}
\alias{fit_bayesian_growth}
\title{Fit Bayesian Individual Growth Models}
\usage{
fit_bayesian_growth(
  lt,
  age,
  sex = NULL,
  female = NULL,
  male = NULL,
  data = NULL,
  model = c("v", "g", "l"),
  k_based = FALSE,
  birth_stanfit = NULL,
  length.mature_stanfit = NULL,
  age.mature_stanfit = NULL,
  Lmax = NULL,
  Linf_multiplier = 1.05,
  CV_Linf = 0.2,
  CV_k = 0.5,
  CV_L0 = 0.3,
  CV_Lmat = 0.2,
  CV_tmat = 0.3,
  prior_L0 = NULL,
  prior_Lmat = NULL,
  prior_tmat = NULL,
  prior_k = NULL,
  use_pooling = TRUE,
  prior_tau = 0.2,
  robust = FALSE,
  loc_sig = 0,
  scale_sig = 1,
  parallel = TRUE,
  chains = 4,
  iter_warmup = 1000,
  iter_sampling = 1000,
  refresh = 500,
  seed = 1234,
  ...
)
}
\arguments{
\item{lt}{Column name in \code{data} or numeric vector of observed lengths (> 0).}

\item{age}{Column name in \code{data} or numeric vector of observed ages (>= 0).}

\item{sex}{Optional column name or vector for sex. If provided, fits two-sex model.
Supports auto-detection of common coding conventions including: F/M, Female/Male,
1/2 (numeric), and equivalents in multiple languages. See \code{\link{fit_bayesian_maturity}}
for full list.}

\item{female}{Optional. Explicit specification of how females are coded in the data.
If \code{NULL} (default), auto-detection is attempted.}

\item{male}{Optional. Explicit specification of how males are coded in the data.
Must be provided together with \code{female}.}

\item{data}{A data.frame or data.table containing referenced columns. May include
incomplete cases (length without age) which are used only to determine Lmax.}

\item{model}{Character. Growth model type: \code{"v"} (von Bertalanffy),
\code{"g"} (Gompertz), or \code{"l"} (Logistic). Default \code{"v"}.}

\item{k_based}{Logical. If \code{TRUE}, uses traditional k-based parameterization.
If \code{FALSE} (default), derives k from maturity parameters (Lmat, tmat).}

\item{birth_stanfit}{Optional CmdStanMCMC from \code{\link{fit_bayesian_birth}}.
Used to set informative priors for L0.}

\item{length.mature_stanfit}{Optional CmdStanMCMC from \code{\link{fit_bayesian_maturity}}
with L50 parameter. Used to set priors for Lmat.}

\item{age.mature_stanfit}{Optional CmdStanMCMC from \code{\link{fit_bayesian_maturity}}
with t50 parameter. Used to set priors for tmat.}

\item{Lmax}{Numeric. Maximum observed length by sex for setting Linf prior and
lower bound. If \code{NULL} (default), computed from all length data in \code{data}
(including rows with missing age). Can be a single value (applied to both sexes)
or length-2 vector for sex-specific values c(female, male).}

\item{Linf_multiplier}{Numeric. Multiplier for Lmax to get Linf prior mean.
Default 1.05 (i.e., 5\% larger than observed max).}

\item{CV_Linf}{Coefficient of variation for Linf prior. Default 0.2.}

\item{CV_k}{Coefficient of variation for k prior. Default 0.5.}

\item{CV_L0}{Coefficient of variation for L0 prior (if not from birth fit). Default 0.3.}

\item{CV_Lmat}{Coefficient of variation for Lmat prior (if not from maturity fit). Default 0.2.}

\item{CV_tmat}{Coefficient of variation for tmat prior (if not from maturity fit). Default 0.3.}

\item{prior_L0}{Manual prior for L0 on natural scale: \code{c(mean, sd)}.
Ignored if \code{birth_stanfit} provided.}

\item{prior_Lmat}{Manual prior for Lmat on natural scale. Only for maturity-based.}

\item{prior_tmat}{Manual prior for tmat on natural scale. Only for maturity-based.}

\item{prior_k}{Manual prior for k on natural scale. Only for k-based models.}

\item{use_pooling}{Logical. For two-sex models, use partial pooling on location
parameters? Default \code{TRUE}. Slope parameters are never pooled.}

\item{prior_tau}{Scale for half-normal priors on between-sex SD. Default 0.2.}

\item{robust}{Logical. Use Student-t observation model? Default \code{FALSE}.}

\item{loc_sig, scale_sig}{Location and scale for Cauchy prior on sigma.
Defaults: \code{loc_sig = 0}, \code{scale_sig = 1}.}

\item{parallel}{Logical. Run chains in parallel? Default \code{TRUE}.}

\item{chains}{Integer. Number of MCMC chains. Default 4.}

\item{iter_warmup}{Integer. Warmup iterations per chain. Default 1000.}

\item{iter_sampling}{Integer. Sampling iterations per chain. Default 1000.}

\item{refresh}{Integer. Progress update frequency. Default 500.}

\item{seed}{Integer. Random seed. Default 1234.}

\item{...}{Additional arguments passed to \code{$sample()}.}
}
\value{
A \code{CmdStanMCMC} object.
}
\description{
Fits von Bertalanffy, Gompertz, or Logistic growth models using precompiled
Stan models via the instantiate package. Supports either traditional k-based
or maturity-based parameterization, and both single-sex and hierarchical
two-sex models with optional partial pooling between sexes.
}
\details{
\subsection{Growth Model Equations}{

\strong{von Bertalanffy (model = "v"):}
\deqn{L(t) = L_\infty - (L_\infty - L_0) e^{-kt}}

\strong{Gompertz (model = "g"):}
\deqn{L(t) = L_\infty \exp\left(-\ln\left(\frac{L_\infty}{L_0}\right) e^{-kt}\right)}

\strong{Logistic (model = "l"):}
\deqn{L(t) = \frac{L_\infty}{1 + \left(\frac{L_\infty}{L_0} - 1\right) e^{-kt}}}
}

\subsection{Prior Specification}{

Priors are constructed using coefficient of variation (CV) arguments:

\strong{Linf:} Prior mean defaults to \code{1.05 * Lmax}, where Lmax is the maximum
observed length (including incomplete cases without age data). The prior SD
is \code{mean * CV_Linf}. Users can override Lmax via the \code{Lmax} argument.

\strong{k:} Prior mean is estimated from the data by solving the growth equation
for k at each observation and averaging. Prior SD is \code{mean * CV_k}.

\strong{L0, Lmat, tmat:} Can be informed by prior birth/maturity model fits, or
specified manually.
}

\subsection{Initialization}{

Initial values are set to data-derived estimates. For k, the Gulland-Holt
linearization method is used to provide a reasonable starting value.
}
}
\examples{
\dontrun{
# Load simulated data
data(growth_data)

# Workflow: birth -> maturity -> growth

# Step 1: Fit birth model
birth_fit <- fit_bayesian_birth(
  embryo_lts = growth_data[embryo == TRUE, fl],
  free_swimming_lts = growth_data[embryo == FALSE, fl]
)

# Step 2: Fit maturity models
mat_data <- growth_data[embryo == FALSE & !is.na(mat)]
L50_fit <- fit_bayesian_maturity(
  maturity = "mat", lt = "fl", sex = "sex",
  data = mat_data, use_pooling = TRUE
)
t50_fit <- fit_bayesian_maturity(
  maturity = "mat", age = "age", sex = "sex",
  data = mat_data[!is.na(age)], use_pooling = TRUE
)

# Step 3: Fit growth model (maturity-based, pooled)
growth_fit <- fit_bayesian_growth(
  lt = "fl", age = "age", sex = "sex",
  data = growth_data[embryo == FALSE & !is.na(age)],
  model = "vb",
  k_based = FALSE,
  birth_fit = birth_fit,
  L50_fit = L50_fit,
  t50_fit = t50_fit,
  use_pooling = TRUE
)

# View key parameters
growth_fit$summary(c("Linf", "L0", "k", "Lmat", "tmat"))

# Example with imbalanced data
data(imbalanced_data)
gdata <- imbalanced_data[embryo == FALSE & !is.na(age)]
growth_imbal <- fit_bayesian_growth(
  lt = "fl", age = "age", sex = "sex",
  data = gdata,
  use_pooling = TRUE  # Important for sparse male data
)
}

}
\seealso{
\code{vignette("fit_bayesian_growth")} for usage examples.

\code{vignette("partial_pooling")} for hierarchical modeling of imbalanced sex ratios.

\href{../doc/Understanding_vitalBayes.html#growth}{Statistical Methods: Growth Models}
for equations and model comparison.

\href{../doc/Understanding_vitalBayes.html#maturity-param}{Statistical Methods: Maturity-Based Parameterization}
for the derivation of k from maturity parameters.

\href{../doc/Understanding_vitalBayes.html#linf}{Statistical Methods: L-infinity Constraint}
for why Linf must exceed maximum observed length.

\code{\link{fit_bayesian_birth}}, \code{\link{fit_bayesian_maturity}},
\code{\link{plot_growth_curve}}, \code{\link{compare_growth_models}}
}
