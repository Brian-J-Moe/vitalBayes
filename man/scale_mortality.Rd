% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_mortality.R
\name{scale_mortality}
\alias{scale_mortality}
\title{Scale Mortality Schedule to Target}
\usage{
scale_mortality(M, M_target = NULL, tmax = NULL, p = 0.001)
}
\arguments{
\item{M}{Numeric vector of instantaneous mortality rates.}

\item{M_target}{Target mean mortality. Can be a numeric scalar, a function
of tmax (e.g., \code{function(tmax) ...}), or NULL to derive from survival
probability \code{p}.}

\item{tmax}{Maximum age (required if M_target is a function or NULL).}

\item{p}{Probability of surviving to tmax. Used only if M_target is NULL.
Default 0.001 (0.1 percent survival to tmax).}
}
\value{
Numeric vector of scaled mortality rates.
}
\description{
Rescales an age-specific mortality schedule so the mean mortality equals a
target value.
}
\details{
The scaling applies: \eqn{M_{scaled} = M_{raw} / \bar{M}_{raw} \times M_{target}}

If \code{M_target = NULL}, it is derived as \eqn{-\ln(p) / t_{max}}.
}
\examples{
\dontrun{
M_raw <- M_chen_watanabe(0:30, Linf = 200, k = 0.15, t0 = -1, tmax = 30)
M_scaled <- scale_mortality(M_raw, M_target = 0.2)

# Using Hoenig-style target
hoenig_target <- function(tmax) 4.899 * tmax^(-0.916)
M_scaled <- scale_mortality(M_raw, M_target = hoenig_target, tmax = 30)
}

}
